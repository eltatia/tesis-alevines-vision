# ============================================================
# Sistema de conteo automático de alevines - dependencias
# Probado con Python 3.10 en Windows
# ============================================================

# --- Deep learning / detección ---
ultralytics==8.3.40

# --- Visión por computador ---
opencv-python==4.10.0.84

# --- Cálculo numérico y datos ---
numpy==1.26.4
pandas==2.2.3

# --- Visualización ---
matplotlib==3.9.2
seaborn==0.13.2

# --- Métricas ---
scikit-learn==1.5.2

# --- Utilidades ---
tqdm==4.66.5
PyYAML==6.0.2
Pillow==10.4.0

# --- Deduplicación perceptual de frames (paso opcional) ---
imagehash==4.3.1

# --- Notebooks (opcional, para 01_exploracion_dataset, etc.) ---
jupyter==1.1.1
ipykernel==6.29.5

# ============================================================
# Notas sobre PyTorch:
# - Este requirements.txt NO instala torch; ultralytics lo arrastra,
#   pero conviene instalarlo PRIMERO con la versión correcta para tu GPU.
#
# - Para CPU (cualquier laptop):
#     pip install torch torchvision
#
# - Para GPU NVIDIA RTX 20/30/40 series (Ampere/Ada):
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# - Para GPU NVIDIA RTX 50 series (Blackwell, sm_120) — incluye RTX 5070, 5070 Ti, 5080, 5090:
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#   ¡IMPORTANTE! El índice cu121 NO funciona con Blackwell.
#
# - Para exportar a TFLite (Fase 14) instalar también:
#     pip install tensorflow==2.17.0
# ============================================================
lapx
sahi
