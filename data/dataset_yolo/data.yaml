# =====================================================
# Configuración del dataset YOLO para detección de alevines
# =====================================================
# Ruta ABSOLUTA para evitar ambigüedad al resolver el dataset
# (Ultralytics resuelve rutas relativas contra su datasets_dir, no contra el cwd).
# =====================================================

path: C:/Users/Usuario/TESIS/data/dataset_yolo  # raíz del dataset (absoluta)
train: images/train
val: images/val
test: images/test

# Número de clases
nc: 1

# Nombres de clases (clase única: alevin)
names:
  0: alevin
